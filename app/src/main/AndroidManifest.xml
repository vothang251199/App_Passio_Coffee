<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_passio_coffee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Passio_Coffee">
        <activity android:name=".TheThanhVien">

        </activity>
        <activity android:name=".DinhVi" />
        <activity android:name=".ThongBao" />
        <activity android:name=".Profile" />
        <activity android:name=".Coupon" />
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".ChonMon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Load"></activity>
        <activity android:name=".Login" />
        <activity android:name=".LoginSDT" />
    </application>

</manifest>